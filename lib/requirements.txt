# ── Core (always required) ────────────────────────────────────────────────────
anthropic>=0.40.0
requests>=2.32.0
python-dotenv>=1.0.0

# ── Per-provider (install only what you use) ──────────────────────────────────
# LLM_PROVIDER=openai / groq / ollama / openai-compat
openai>=1.0.0

# LLM_PROVIDER=gemini
google-genai>=1.0.0

# LLM_PROVIDER=bedrock  (boto3 is large; comment out if not using AWS)
boto3>=1.26.0

# ── Project libraries ─────────────────────────────────────────────────────────
duckdb>=1.1.0
fastapi>=0.115.0
matplotlib>=3.9.0
pandas>=2.2.0
psycopg2-binary>=2.9.0
reportlab>=4.0.0
uvicorn>=0.32.0
