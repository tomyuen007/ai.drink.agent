# ── Core (always required) ────────────────────────────────────────────────────
anthropic>=0.25.0

# ── RAG (rag.py is imported directly — must be in this venv) ─────────────────
# Install torch CPU-only FIRST before this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
chromadb>=0.5.0
sentence-transformers>=3.0.0
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
mcp>=1.0.0
mangum>=0.17.0

# ── Per-provider (install only what you use) ──────────────────────────────────
# LLM_PROVIDER=openai / groq / ollama / openai-compat
openai>=1.30.0

# LLM_PROVIDER=gemini
google-genai>=1.0.0

# LLM_PROVIDER=bedrock  (boto3 is large; comment out if not using AWS)
boto3>=1.26.0
